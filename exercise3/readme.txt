To compile/run the code for exercises 3.1 - 3.2 do the following:

1. Open the folder '/exercise3_x' in your terminal (replace 'x' with either '1' or '2' depending on which program you want to run).
2. Enter the command 'sbt run' run the program.

